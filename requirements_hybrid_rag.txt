# Hybrid RAG Dependencies
# These are additional requirements for the hybrid RAG functionality

# Core libraries (should already be installed)
langchain>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5
chromadb>=0.4.0
openai>=1.0.0

# Document processing
pdfplumber>=0.9.0
tabula-py>=2.8.0
pytesseract>=0.3.10
pillow>=10.0.0
pypdf>=3.0.0
python-docx>=0.8.11
unstructured>=0.10.0

# Graph processing
networkx>=3.0
python-louvain>=0.16  # For community detection

# LangGraph for routing
langgraph>=0.0.20

# Machine learning and embeddings
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
torch>=2.0.0  # For advanced reranking
transformers>=4.30.0  # For CLIP, BLIP models (multimodal)
sentence-transformers>=2.2.0  # For cross-encoder reranking

# Azure integration
azure-storage-blob>=12.0.0
azure-ai-formrecognizer>=3.3.0  # For Azure OCR
azure-cognitiveservices-vision-computervision>=0.9.0

# Reranking
flashrank>=0.2.0  # Fast reranking

# RAPTOR
umap-learn>=0.5.0  # For RAPTOR clustering
hdbscan>=0.8.0  # For RAPTOR clustering

# Audio/Video processing (optional for multimodal)
# whisper>=1.0.0  # For audio transcription
# opencv-python>=4.8.0  # For video processing

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
tqdm>=4.65.0

# OCR dependencies
# Note: Tesseract OCR must be installed separately on the system
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Linux: apt-get install tesseract-ocr
# Mac: brew install tesseract